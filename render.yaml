services:
  - type: web
    name: anlconstructions-timetracker
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node api.js
    envVars:
      - key: NODE_ENV
        value: production
